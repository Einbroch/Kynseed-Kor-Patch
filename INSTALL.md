# Kynseed 한국어 패치 — 설치 안내

> **v1.1.1** (2026-09-15) — 실기에서 한글이 안 나오던 원인을 고쳤습니다.
> 이전 버전을 넣어 두셨다면 폴더를 덮어쓰세요. **v1.1.0 이하를 쓰고 계시면
> 반드시 갱신하셔야 합니다.**

## 대상

| 항목 | 값 |
|---|---|
| 게임 | Kynseed (Nintendo Switch) |
| Title ID | `0100783020A1A000` |
| **필요 버전** | **업데이트 1.0.0.54 (v262144) 적용본** |
| 리전 | US |

> **업데이트를 반드시 먼저 설치하세요.** 이 패치는 1.0.0.54 기준으로 만들었습니다.
> 베이스(1.0.0.39)만 있는 상태에서 넣으면 텍스트가 어긋나거나 글자가 빠질 수 있습니다.

## 패키지

| 파일 | 크기 | SHA-256 |
|---|---:|---|
| `0100783020A1A000.zip` | 3,046,632 | `880BE37B7650BA638C65A49CFE6CE552ABE583974D57D5A314CABE9A1593C739` |
| `0100783020A1A000-ENslot.zip` (선택) | 3,704,469 | `F5E1B0129AE492A651B69333B458543BDB2B4DC2B16E2F65C7AE2DA41182CFC6` |

**어느 쪽을 받아야 하나요?**

| 상황 | 받을 것 |
|---|---|
| 처음 시작하거나, 이미 한국어로 나오던 경우 | `0100783020A1A000.zip` |
| 영어로 플레이하던 세이브가 있는 경우 | `0100783020A1A000-ENslot.zip` |

게임은 **세이브에 언어를 저장**하고 게임 안에는 언어를 바꾸는 항목이 없습니다.
영어로 하던 세이브가 있으면 기본 패키지로는 계속 영어가 나옵니다. EN 슬롯 포함판은
영어 슬롯에도 같은 한국어를 넣어 두므로 세이브를 건드리지 않아도 됩니다
(대신 넣어 둔 동안 영어로 되돌릴 수 없습니다).

ZIP 안에는 `romfs/` 폴더 하나만 들어 있고, 그 아래 **패치 파일 69개**만 있습니다.
원본 게임 데이터·키·펌웨어는 포함되어 있지 않습니다.

```
romfs/
├─ Data/Text/KO/              (63개 — 한국어 텍스트)
└─ Graphics/
   ├─ Splash/load_chicken.png (로딩 애니메이션)
   └─ SpriteSheets/HandMade/  (폰트 아틀라스 2개 + 이미지 3개)
```

## 설치

### 실기 (Atmosphère)

SD 카드의 다음 위치에 `romfs` 폴더를 넣습니다.

```
/atmosphere/contents/0100783020A1A000/romfs/
```

즉 ZIP 안의 `romfs/Data/...` 가 `/atmosphere/contents/0100783020A1A000/romfs/Data/...` 가
되면 됩니다. **`0100783020A1A000` 폴더를 한 번 더 만들지 마세요.**

구버전 Atmosphère(0.10 이전)나 SX OS 는 폴더 이름이 다릅니다.

| 환경 | 경로 |
|---|---|
| Atmosphère 0.10 이상 | `/atmosphere/contents/0100783020A1A000/romfs/` |
| Atmosphère 0.10 미만 | `/atmosphere/titles/0100783020A1A000/romfs/` |
| SX OS | `/sxos/titles/0100783020A1A000/romfs/` |

**Title ID 는 업데이트 쪽(`...A800`)이 아니라 본편(`...A000`)입니다.** 업데이트를
설치한 상태여도 본편 ID 폴더에 넣는 것이 맞습니다.

#### 실기에서 영어로 나올 때

| 증상 | 원인 | 조치 |
|---|---|---|
| `loading` (영문) + 영어 메뉴 | 모드 미적용 | 위 경로와 CFW 종류를 확인하세요 |
| `로딩` (한글) + 영어 메뉴 | 세이브의 언어값이 `EN` | 아래 두 방법 중 하나 |
| 한국어가 `□` | 폰트 파일 미적용 | `font.png`·`font.json` 경로 확인 |

`로딩` 은 한글인데 메뉴만 영어라면 패치는 이미 정상 로드된 상태입니다. 게임이
세이브에 저장해 둔 언어값이 `EN` 이라 영어 텍스트를 읽고 있을 뿐입니다.

**방법 1 — EN 슬롯 포함판으로 교체 (쉬움)**

`0100783020A1A000-ENslot.zip` 을 받아 같은 자리에 덮어씁니다. 세이브를 건드리지
않아도 한국어가 나옵니다.

**방법 2 — 세이브의 언어값을 고치기**

JKSV·Checkpoint 같은 세이브 관리자로 Kynseed 세이브를 SD 카드에 백업한 다음,
백업 폴더의 `ConfigSetup.ini` 에서 `Language=EN` 을 `Language=KO` 로 바꾸고 다시
복원합니다. **복원은 기존 세이브를 덮어쓰므로 백업본을 따로 보관하세요.**

### PC 에뮬레이터 (Eden / Ryujinx 계열)

게임 목록에서 Kynseed 우클릭 → 모드 폴더 열기 → 아래 구조로 넣습니다.

```
load/0100783020A1A000/<원하는 이름>/romfs/
```

예: `load/0100783020A1A000/Korean/romfs/Data/Text/KO/...`

### Android Eden

Android에서는 수동으로 `Android/data` 아래에 복사하지 말고 Eden의 게임별 애드온
가져오기를 사용하세요. RomFS 모드 가져오기 문제가 수정된 **Eden Stable v0.2.1 이상**을
권장합니다.

1. `0100783020A1A000.zip`을 압축 해제해 `Kynseed_KOR` 폴더를 만듭니다.
2. 폴더 구조가 `Kynseed_KOR/romfs/Data/Text/KO/...`인지 확인합니다.
3. Eden에서 Kynseed를 길게 누릅니다.
4. `애드온(Add-ons) → + → Mods and Cheats`를 선택합니다.
5. ZIP이나 `romfs` 자체가 아니라, 바로 아래에 `romfs`가 있는 **`Kynseed_KOR` 폴더**를
   선택합니다.
6. 애드온 목록에서 해당 모드가 활성화됐는지 확인하고 Eden을 완전히 다시 시작합니다.

```text
Kynseed_KOR/
└─ romfs/
   ├─ Data/Text/KO/Text_EN_Tags.txt
   └─ Graphics/SpriteSheets/HandMade/
      ├─ font.png
      └─ font.json
```

### 한국어 슬롯 선택 (필수)

이 패치는 게임의 **한국어 슬롯을 교체**하는 방식입니다. 한국어가 선택되어 있지 않으면
패치가 화면에 나타나지 않습니다.

- **실기**: 본체 설정 → 본체 → 언어 → **한국어**, 지역 → **한국**
- **에뮬레이터**: 시스템 설정에서 지역 `한국`, 언어 `한국어`

Kynseed는 시스템 언어와 별도로 세이브 데이터의 `ConfigSetup.ini`에 선택 언어를
저장하며, 게임 안 설정 화면에는 언어 변경 항목이 없습니다. 시스템 언어를 한국어로
맞춰도 이 값이 `EN`이면 메뉴는 영어로 나옵니다.

#### Android Eden에서 메뉴만 영어로 나올 때

한글로 `로딩`이 표시되는데 메뉴만 영어라면 패치는 이미 정상 로드된 상태입니다.
다음 절차로 게임의 언어값만 바꾸면 됩니다.

1. Kynseed를 완전히 종료합니다.
2. Kynseed를 길게 누르고 `게임 속성 → 저장 데이터 → 내보내기`를 선택합니다.
3. 내보낸 ZIP을 별도로 백업한 뒤 압축을 풉니다.
4. `0100783020A1A000/ConfigSetup.ini`에서 `Language=EN`을 `Language=KO`로 바꿉니다.
5. ZIP 최상위에 Title ID 폴더가 바로 오도록 다시 압축합니다.

```text
Kynseed-save-KO.zip
└─ 0100783020A1A000/
   ├─ ConfigSetup.ini
   └─ (기존 세이브 파일)
```

6. `게임 속성 → 저장 데이터 → 가져오기`에서 수정한 ZIP을 선택합니다.
7. Eden을 강제 종료한 뒤 다시 실행합니다.

저장 데이터 가져오기는 기존 세이브를 덮어쓰므로 원본 내보내기 ZIP을 반드시
보관하세요.

#### 증상별 확인

| 화면 | 의미 | 조치 |
|---|---|---|
| `loading` + 영어 메뉴 | 모드 미적용 | 애드온 설치 경로와 활성화 상태 확인 |
| `로딩` + 영어 메뉴 | 모드 적용, 언어값 `EN` | `ConfigSetup.ini`를 `Language=KO`로 변경 |
| `로딩` + 한국어 메뉴 | 정상 | 설치 완료 |
| 한국어가 `□`로 표시 | 폰트 파일 미적용 | `font.png`와 `font.json` 경로 확인 |

## 제거

넣었던 `romfs` 폴더(또는 모드 폴더)를 지우면 됩니다. 이 패치는 원본 파일을 고치지 않는
overlay 라서, 지우면 원래 상태로 완전히 돌아갑니다. 세이브 데이터에도 영향이 없습니다.

## 알려진 제한

| # | 내용 |
|---|---|
| 1 | 에뮬레이터(Eden)에서 타이틀·설정·인트로·인게임·힌트 화면을 확인했습니다. 다만 상점·게시판·판매대 등 진행이 필요한 화면은 아직 못 봤습니다 |
| 2 | 업데이트 1.0.0.54 전용입니다. 다른 버전에서는 동작을 보장하지 않습니다 |
| 3 | 일부 고유명사가 영문으로 남아 있습니다 (97곳). 게임이 원래 배포한 공식 한국어와 같은 지점입니다 |
| 6 | v1.1.1 은 `BigGlyphs` 를 원본 KO 값 `1` 로 되돌립니다. 글자 크기·줄 간격이 v1.1.0 과 달라 보일 수 있습니다 |
| 5 | v1.1.0 의 주민 대사 어조 교정문은 정적 검사만 통과했고, 아직 화면에서 확인하지 못했습니다 |
| 4 | 게임이 언어를 세이브 데이터에 따로 저장합니다. 한글 `로딩` 뒤 영어 메뉴가 나오면 위 Android 절차에 따라 `ConfigSetup.ini`를 `Language=KO`로 바꾸세요 |

문제가 생기면 넣은 폴더를 지우고 원본으로 돌아간 뒤, 어떤 화면에서 무엇이 이상했는지
알려주세요.
