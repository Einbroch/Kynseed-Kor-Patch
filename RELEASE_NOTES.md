# Kynseed 한국어 패치 — 릴리스 노트

| 항목 | 값 |
|---|---|
| 버전 | `v1.1.0` |
| 날짜 | 2026-09-14 |
| build_id | `FINAL` |
| canonical | `0100783020A1A000.zip` (3,046,640 bytes) |
| SHA-256 | `686CD1DD86F383946E9C692CA25575B4B6CA6B44E54387D4820E6301852EF051` |
| 대상 | Kynseed (NSW) `0100783020A1A000` + 업데이트 1.0.0.54 |
| 상태 | **PASS (bench) + PASS (runtime, PC/Android 부분)** — 아래 §6 |

## 1. v1.1.0에서 바뀐 것 — 주민 말투

"주민마다 성격이 다른데 대사 말투가 다 똑같다"는 지적을 받아 **발화 4,799행 전체**를
다시 손봤습니다. 실제로 재어 보니 결함이 세 가지였습니다.

| 결함 | v1.0.1 실측 | v1.1.0 |
|---|---|---|
| 한 주민이 줄마다 말투를 바꿈 | 성격 5종이 **전부** 해요체·반말·합쇼체 세 가지를 섞어 씀 | 성격 × 관계 칸마다 하나로 통일 |
| 관계가 깊어져도 말투가 안 바뀜 | 존댓 비율이 Dislike 53% / Neutral 85% / Friendly 43% / Like 30% / Love 70% — 단계와 아무 상관이 없음 | Dislike·Neutral·Friendly=존댓, Like·Love=반말 |
| 인사말이 명사구로 번역됨 | `많은 인사`, `축복받은 인사` 등이 262행 앞에 그대로 붙음 | 인사 표현으로 교체 |

게임은 주민 대사를 `성격(5) × 활동(10) × 관계(5)` 세 축으로 조합합니다. 이 축을 그대로
번역 규칙으로 삼았습니다.

| 성격 | 1인칭 | 말버릇 |
|---|---|---|
| 허풍 (Boastful) | 나/내 | 단정·과장 — `~죠`, `~거든요`, `~걸요` |
| 불평 (Complainer) | 나/내 | 투덜 — `~네요`, `~라니까요` |
| 쾌활 (Jovial) | 나/내 | 감탄·권유 — `~요!`, `~할래요?` |
| 독실 (Pious) | **저/제** | 기원문 — `~하기를 바라요` |
| 괴짜 (Quirky) | 나/내 | 엉뚱 — `~거든요`, `~더라고요` |

관계가 깊어지면 주민이 말을 놓습니다(사용자 결정). Like·Love 단계에서 반말로 바뀝니다.

교정 결과를 기계로 검사합니다(`check_voice.py`, 6개 규칙).

| 검사 | v1.0.1 | v1.1.0 |
|---|---:|---:|
| 관계 단계가 요구하는 말높임 위반 | 1,516 | 0 |
| 같은 칸 안에서 말높임 혼용 | 98 | 0 |
| 성격별 1인칭 불일치 (독실=저/제, 나머지=나/내) | 184 | 0 |
| `당신` 사용 | 321 | 0 |
| 인사말 풀 말높임 불일치 | 13 | 0 |
| **합계** | **2,132** | **0** |

v1.0.1 수치는 배포된 `Kynseed.zip` 을 다시 풀어 같은 검사기로 잰 값입니다.

관계 단계별 최종 분포입니다(조각 = 종결어미가 없는 짧은 대사).

| 단계 | 행 | 말높임 |
|---|---:|---|
| Dislike | 121 | 해요체 72 / 조각 49 |
| Neutral | 140 | 해요체 114 / 조각 26 |
| Friendly | 383 | 해요체 270 / 조각 113 |
| Like | 499 | 반말 315 / 조각 184 |
| Love | 153 | 반말 69 / 조각 84 |

고쳐 쓴 문장에 새 글자가 생겨 **한글 글리프 27자를 아틀라스에 더 넣었습니다**
(83 → 110자, 프레임 5,889 → 5,916). 기존 글리프는 그대로입니다.

## 2. 이 패치의 성격

Kynseed Switch판은 **이미 공식 한국어를 탑재**하고 있습니다. 다만 그 번역은 기계번역
품질이라 고유명사가 그대로 남고 어조가 들쭉날쭉합니다.

이 패치는 없는 한국어를 새로 만든 게 아니라, **기존 한국어 슬롯을 고품질 번역으로
교체**합니다.

## 3. 번역 범위

### 텍스트 — 21,000행 / 63파일 (100%)

| 구분 | 행 수 | 내용 |
|---|---:|---|
| 사용자 확정 번역 이식 | 17,806 | 사용자가 다듬은 개선판을 그대로 이식 |
| 토큰·오타 교정 | 13 | 이식 과정과 **실행 검증**에서 발견한 실제 결함 |
| **신규 번역** | **3,186** | 나머지 62개 파일 + 업데이트 신규 콘텐츠 |
| **어조 교정 (v1.1.0)** | **1,835** | 위 두 갈래에 걸쳐 있는 주민 발화. §1 참조 |

교정한 13건 중에는 문장이 `말했ㅓ.` 처럼 완성형이 아닌 홑모음으로 끝나 글자가 아예
사라지던 것, 게임 인물 `Pott`(브라우니)이 영문으로 남아 있던 것이 있습니다.

실행 검증에서 **UI 라벨 5건**을 추가로 잡았습니다. 공식 한국어의 기계번역이 품사 태그를
번역어에 붙인 채 남긴 것으로, 화면에 `명사피부` 로 그대로 떠 있었습니다.

| 키 | 원문 | 결함 | 교정 |
|---|---|---|---|
| `UI_Character_Skin` | `Skin` | `명사피부` | `피부` |
| `UI_Character_Home` | `Home` | `명사집` | `집` |
| `UI_Craft_Button` | `Craft` | `동사제작` | `제작` |
| `UI_CraftCost` | `Craft ☎<VAR1>` | `동사제작 ☎<VAR1>` | `제작 ☎<VAR1>` |
| `UI_Chat_Gift` | `Gift` | `동사선물` | `선물` |

번역하지 않고 남긴 것: 엔진 지시어(`PAGEBREAK` 등), URL, 크레딧 실명, 저작권 고지,
농담이 약어 자체인 `G.O.A.T`.

### 폰트 — 한글 110자 추가

게임의 한국어 폰트는 비트맵 아틀라스(`font.png` + `font.json`)이고 한글 1,293자가
들어 있었습니다. 새 번역이 쓰는 글자 중 **110자가 빠져 있어** 추가했습니다
(v1.0.1 에서 83자, v1.1.0 의 어조 교정으로 27자 추가).

- 원본 폰트를 Malgun Gothic 16px 무 안티앨리어싱으로 특정 (기존 1,293자 중 1,233자 픽셀 완전 일치)
- 셀 규칙을 기존 1,291자 전수 실측으로 확정 (좌 여백 2, 우 여백 3, 셀 높이 21, 예외 0)
- 아틀라스 2048×1395 → 2048×1416, 프레임 5,806 → 5,916
- **기존 글리프의 픽셀·좌표는 한 개도 바뀌지 않았습니다** (일본어·영문 포함)
- 최종 번역문의 **한글 미커버 0**

### 이미지 — 6건

게임 전체 이미지 263개 / 프레임 102,339개를 눈으로 전수 검사해 문자 삽화 6건을
찾아 다시 그렸습니다.

| 원문 | 한국어 | 위치 |
|---|---|---|
| `loading` | 로딩 | 로딩 화면 |
| `THE BIG BUILD UPDATE` | 대규모 건축 업데이트 | 타이틀 배너 |
| `MISSING` / `REWARD` | 실종 / 현상금 | 실종 포스터 |
| `WANTED` | 지명수배 | 수배 전단 |
| `Price` / `Amount` / `Total:` | 가격 / 수량 / 합계: | 판매대 영수증 |
| `Shop Fund` | 상점 자금 | 리본 라벨 |

원본 팔레트를 그대로 썼고(픽셀아트 3파일 **새 색 0개**), 아틀라스의 나머지 1,158
프레임은 픽셀 단위로 원본과 동일합니다.

번역하지 않은 문자 이미지: 게임 제호 `Kynseed`, 개발사 로고, 컨트롤러 버튼 각인
(A/B/ZL/R2 등), `XP` 아이콘(본문이 XP 표기를 쓰므로 통일).

## 4. 검증 (정적 / bench)

전 항목 통과 (`errors: 0`). **빌드에 실제로 들어간 바이트**로 검사했습니다.

| 검사 | 결과 |
|---|---|
| 텍스트 63파일 파싱·왕복 | 전부 통과 |
| 보호 필드(구분자·키·태그) 무변경 | 불일치 0 |
| 빌드 텍스트 ↔ 번역 시트 대조 | **21,000행** exact 일치 |
| 제어 토큰 손실 | 0행 |
| 폰트 커버리지 (빌드 아틀라스 재추출) | 한글 미커버 **0** |
| 아틀라스 프레임 수 (원본 5,806 + 선언 110) | 5,916 일치 |
| 아틀라스 프레임 rect 유효성 | 위반 0 |
| 어조 게이트 6종 (`check_voice.py`) | 발화 4,799행, 위반 **0** |
| 이미지 캔버스·모드·비대상 프레임 | 전부 보존 |
| ZIP 결정성 | 두 번 조립해 바이트 동일 |
| ZIP 재열기·전개 | entry 69, payload 전수 일치, CRC 통과 |

## 5. 시험 환경

| 항목 | 값 |
|---|---|
| 정적 검증 | Python (자체 검증기 `verify_build.py`) — errors 0 |
| 에뮬레이터 | PC Eden `master-a43664c0fd` + Android Eden 사용자 실기 확인 |
| 게임 버전 | Update 1.0.0.54 적용 확인 |
| 실기 | **미실행** |

## 6. 상태 — 꼭 읽어주세요

```
PASS (bench)          정적·구조·폰트·이미지·왕복 검증 전부 통과
PASS (runtime, 부분)   에뮬레이터에서 아래 화면을 실제로 확인 (v1.0.1 빌드 기준)
PENDING (hardware)    실기(Atmosphère) 검증 미수행
```

> **v1.1.0 의 어조 교정문은 아직 화면에서 보지 못했습니다.** 바뀐 것은 텍스트 내용과
> 한글 글리프 27자이고 파일 구조·개수·주입 방식은 v1.0.1 과 같습니다. 새 글자 27자는
> 아틀라스 왕복 재추출과 렌더 probe 로 검사했지만, 실제 대사 화면에서 본 것은
> 아닙니다.

| 항목 | 결과 |
|---|---|
| 로딩 화면 `로딩` 이미지 | 정상 (잘림·잔재 없음) |
| **인트로 시** | **한국어 텍스트로 출력** — 영문 이미지가 아닙니다 |
| 타이틀·일시정지 메뉴 | 정상 |
| 설정 4개 탭 전 항목 | 정상, 잘림 0 |
| 캐릭터 생성·이름 입력 | 정상 |
| 인게임 튜토리얼 다중행 | 정상 |
| 힌트 책 장문 레이아웃 | 정상 |
| 문장 속 링크(파란 글씨) | 정상 |
| 한글+라틴 혼용 한 줄 | 정상 |
| 폰트 두부·빈 글리프·잘린 받침 | **0건** |
| Android Eden `로딩` 이미지 | 정상 — LayeredFS 모드 로드 확인 |
| Android Eden 메뉴 | `ConfigSetup.ini`의 `Language=KO` 적용 후 한국어 정상 출력 |

캡처 27장을 증거로 보관합니다.

### Android Eden 설치 주의사항

Android에서 한글 `로딩`이 나오지만 메뉴가 영어라면 패치 실패가 아닙니다. Kynseed가
세이브 데이터에 `Language=EN`을 저장해 `Data/Text/EN` 슬롯을 읽는 상태입니다.
게임별 저장 데이터를 내보내 `0100783020A1A000/ConfigSetup.ini`의 값을
`Language=KO`로 바꾼 뒤 다시 가져오면 메뉴까지 한국어로 출력됩니다. 자세한 절차와
ZIP 구조는 동봉된 `INSTALL.md`에 기록했습니다.

### 아직 못 본 것

| 항목 | 이유 |
|---|---|
| **v1.1.0 어조 교정 대사** | 주민과 실제로 대화해야 합니다. 정적 게이트는 전부 통과 |
| 상점·게시판·판매대 이미지 4건 | 게임 진행이 필요합니다 |
| 타이틀 배너 이미지 | 타이틀 화면에 표시되지 않았습니다. 이 빌드에서 안 쓰일 가능성 |
| `↩` 기호 | 스탯 화면에서만 쓰이는데 도달하지 못했습니다 |
| 긴 대사 줄바꿈 66행 | 해당 대사 지점까지 진행이 필요합니다 |
| 실기 동작 | 하드웨어 없음 |

마지막 두 항목은 게임이 출시한 공식 한국어에도 같은 특성이 있어, 문제가 있더라도
이 패치가 만든 회귀는 아닙니다.

## 7. 남은 개선 여지

| 항목 | 내용 |
|---|---|
| 영문 잔존 97곳 | 공식 한국어와 동일한 지점. 이식 범위라 임의로 바꾸지 않았습니다. 어조 교정으로 문장을 다시 쓰면서도 인물·지명 표기는 원래 쓰던 영문을 유지했습니다 |
| 여신 이름 표기 | UI 라벨은 영문(`Druida`), 산문은 음차(`드루이다`). 통일 여부 판단 필요 |
| 반려동물 이름 | `Filifryth` / `Bumbabloopfnoop` — UI 영문 vs 인물 항목 음차 |
| `Brass` ↔ `brass` | 금속 아이템과 화폐가 같은 낱말 |

## 8. 크레딧

- 번역 원본: 사용자 확정 번역 (`Text_EN_Tags.txt`)
- 신규 번역·폰트 확장·이미지 작업·검증: Claude (game-translate 파이프라인)
- 원작: PixelCount Studios Limited

이 패치는 게임을 보유한 사용자가 자신의 사본에 적용하는 용도입니다. 원본 게임 데이터는
포함되어 있지 않습니다.
